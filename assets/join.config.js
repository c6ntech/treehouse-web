/* =========================================================================
   Treehouse /join — 可調設定檔（非工程背景可直接改這裡的值）

   改完存檔即生效。JOIN_CONFIG 要維持合法 JavaScript 語法（字串記得用引號包好、
   逗號別漏）——打錯一個地方會讓整支檔案讀取失敗，join.js 會退回內建預設值，
   不會讓整頁掛掉，但你的修改也不會生效。建議改完後重新整理頁面，確認畫面數字
   有跟著變再關掉分頁。
   ========================================================================= */
window.JOIN_CONFIG = {

  // ---- 報名人數看板 ----
  signup: {
    mode: "sheet",           // "sheet" = 讀 Google Sheet 即時值（正式模式）。"mock" = 只顯示 baseValue，完全不對外連線
    // 只是後備值：mode:"mock" 時的顯示值，或 mode:"sheet" 抓取失敗、而且這個瀏覽器
    // 也沒有 localStorage 快取（＝第一次進站的人）時的最後防線。
    // 正常情況畫面上的數字來自下面那份 Sheet 的 B1，不是這裡。
    //
    // ⚠️ 這個值要「貼近當下真值」，不是對齊 Sheet 的 B2 起算值。
    // 2026-08-22 之前這裡是 113（＝B2），真值已經 310，抓取失敗那一刻訪客就看到 113，
    // 明顯是錯的。現在改成每次跑 tools/build-og-image.sh 重截分享卡時自動對齊當下真值
    // （腳本會讀 __JOIN_DEBUG__.lastGoodFetch.sheet），所以這裡通常不用手改。
    // 要調整起算人數仍然是改 Sheet 的 B2。
    baseValue: 1141,
    round: "floor",             // "floor" 無條件捨去到整數；"none" 不處理（Sheet 應該本來就是整數）

    // ⚠️ 這份試算表會被公開讀取（Google 的 gviz 端點要求整份表可公開存取）。
    // 絕對不可以換成表單回應原始試算表的網址 —— 那份含報名者 IG ID、性別、年齡，
    // 一旦被公開讀取就是個資外洩。
    //
    // 目前填的是專用的「for Web Stats」儀表板表：整份只有 A1:B3 三列
    //    （signup_count / 起算值 / 新表單報名數），B1 是頁面實際讀的那一格。
    //    新表單報名數由 IMPORTRANGE 從私有的表單回應表拉「已算好的一個數字」進來，
    //    公開的這份不含任何個資。共用權限必須維持「知道連結的任何人 → 檢視者」，
    //    給到編輯者的話別人可以自己寫 IMPORTRANGE 把私有表整包拉出來。
    sheetUrl: "https://docs.google.com/spreadsheets/d/105ymHSvKcdbQ33xiKtSXF0DPkltpWZYmR2frPQCMTPs/edit",
    pollMs: 600000               // 10 分鐘重新抓一次
  },

  // ---- 男女比例條 ----
  // 顯示的是「派發進 Beta 的男女比」，不是表單填答者的性別分布。
  genderRatio: {
    enabled: true,

    // "policy" = Beta 期間用。App 內的自動性別平衡還沒開，實際上是手動依計畫書
    //            控制派發（目標 女55：男45）。
    //            ⚠️ 2026-08-26 起畫面顯示的是人工寫死的固定值，**不是**那個派發政策，
    //               也不是從它推導出來的。這段敘述在切回浮動模式前都不成立。
    //            不對外連線，比例由報名人數推導（見下面三個參數）。
    // "csv"    = 外包 CSV 到位後改用這個，讀 App 後台的真實數字。
    mode: "policy",

    // ⚠️⚠️ 目前是「固定值」模式，不是浮動帶寬 ⚠️⚠️
    //
    // 目前用 steps 階梯表（見下方）。若把 steps 設成 null，則 femaleMin 與 femaleMax
    // 設成同一個數就固定顯示該值，
    // 永遠不動。原理：policyFemalePct() 開頭有 `if (hi <= lo) return Math.round(lo);`，
    // 兩端相等就直接早退回傳該值，完全不進遊走迴圈。實測 n=0~50000（含 NaN／負數／
    // Infinity）輸出恆為該值。
    //
    // ⏳ **這些都是 Morgan 指示的暫時值，不是最終數字。**
    //    沿革：2026-08-26 18:30 固定 66 → 20:41 改 63 → 20:44 改成階梯表。
    //    他會依實際招募狀況再指示要調成多少、或何時切回浮動。
    //    接手的人看到這裡不要自己改，也不要以為這個數字有演算法上的依據 —— 沒有，
    //    就是人工指定的一個數字。要改就是改下面兩個值（兩個要一起改成同一個數）。
    //
    // 要回到浮動：把下面兩個值改回 femaleMin: 51 / femaleMax: 53（或其他不含 50 的
    // 帶寬），遊走邏輯就會自動恢復，join.js 不用動。以下註解描述的是浮動模式的行為，
    // 固定值模式下不適用，但保留著供之後切回時參考。
    //
    // ⚠️ 固定值高於 55 會踩到兩件事，切回浮動前請記得：
    //    1. 顯示值高於 55 時（階梯表前四階都是）data-status 會變成 "paused_female"
    //       （目前全 repo 沒有任何 CSS/JS 讀它，畫面沒有表現；csv 模式上線後才有影響）。
    //    2. 頁面上緊鄰的文案是「樹屋會自動維持接近 1:1 的性別平衡」，
    //       階梯表的值都不是 1:1。Morgan 2026-08-26 已把文案改成「樹屋 App 正式推出後，
    //       會自動維持接近 1:1 的性別平衡」，把承諾移到未來式化解，
    //       所以這是已知且已拍板的取捨，不要再提報一次。文案在 join/index.html。
    //
    // ── 以下是浮動模式（femaleMin != femaleMax）的說明 ────────────────────
    //
    // ⏸ 階梯表已停用。設成 null 就完全不生效，下面的浮動帶寬接手。
    //    要改回依報名人數階梯式指定，格式 [報名人數門檻, 女性百分比] 由小到大，
    //    例如 [[757,65],[767,64],[777,63],[787,62],[797,61],[800,60],[810,59],[820,58]]。
    steps: null,

    // policy 模式的浮動帶寬（女性百分比，男性自動 = 100 - 女性）。
    // 52–53：女性 52~53，換算男性 47~48。Morgan 2026-09-04 決定。
    //
    // ⚠️ 只有兩個值，所以**不是隨機遊走，是固定交替**：52→53→52→53…
    //    原因是演算法的候選一定排除前一個值，帶寬剩兩格時就只剩一個候選。
    //    分佈剛好 50/50，跳動幅度固定 1。
    //    要恢復隨機性就把帶寬拉成三格以上（例如 52–54），三個值時每格
    //    有兩個候選，路徑才不可預測。
    femaleMin: 52,
    femaleMax: 53,      // ← 與 femaleMin 相等 = 固定值模式。浮動請用 53

    // 每多少人報名換一次數字。停留長度就是這個值，不多不少。
    changeEverySignups: 10,

    // 換值時機的相位偏移，讓切換點落在指定的人數上。
    // 沒有它時桶界一律是 changeEverySignups 的倍數（10 → 1230 / 1240 / 1250）；
    // 設 3 就變成 **1233 / 1243 / 1253**。Morgan 2026-09-04 指定。
    // 純粹是相位，不影響停留長度、跳動幅度或分佈。設 0 或不設 = 舊行為
    //（已驗證 n=0~30000 逐點相同）。
    changeEveryOffset: 3,

    // 女性那側的權重。
    //
    // ⚠️ **在目前的 51–53 帶寬下，這個值完全沒有作用，改它不會有任何效果。**
    //    join.js 的加權寫法是 `cand > 50 ? 1 + bias : 1` —— 帶寬全在 50 以上時
    //    所有候選值的權重都是 1 + bias，等比例的權重等於沒有權重。
    //    2026-08-26 實測 bias = 0 / 0.5 / 1.0 / 2.5 / 100，n=0~30000 輸出完全相同。
    //
    //    留著 1.0 不刪，是因為帶寬一旦調回跨 50（例如 femaleMin 改回 49）
    //    它就會重新生效。那時的實測對照表（49–53、2026-08-21、n=130~1500）：
    //
    //      bias   男性過半的時間   男性過半會連續多久（中位／最長）
    //      0      11%              5 ／ 5 人
    //      0.5    8%               5 ／ 5 人
    //      1.0    5%               5 ／ 5 人
    //
    //    帶寬若再改，這張表要重跑（做法見 join/HANDOFF.md 的性別比那節）。
    //
    // 這個值在任何帶寬下都不影響停留長度 —— 那項固定由 changeEverySignups 決定。
    // 跳動幅度也不受它影響，但跳動幅度並不是 changeEverySignups 決定的，
    // 而是 join.js 的 ±2 候選窗（for var d = -2; d <= 2）加上帶寬寬度：
    // 帶寬若收成 51–52，跳動就只剩 1 個百分點，changeEverySignups 完全沒動。
    femaleBias: 1.0,

    csvUrl: null,                 // mode:"csv" 才用。欄位固定：統計時間、異性戀男性人數、異性戀女性人數、總註冊人數
    // csv 模式下男女佔比的分母 = 異性戀男性人數 + 異性戀女性人數，不是「總註冊人數」——
    // 總註冊人數可能包含非異性戀使用者，拿它當分母會讓算出的
    // paused_male / paused_female 跟 App 內實際的性別平衡擋人邏輯對不上。
    pauseThreshold: 0.55,          // 任一性別佔比超過這個門檻，該性別就標記暫停開放
    pollMs: 600000
  },

  // ---- 「Treehouse 沒有免費版本」定價說明（Public Release 才開；先建好，預設關閉） ----
  pricing: {
    enabled: false,
    amountLabel: "NT$240",        // 月費金額文案
    trialDuration: "30 天"         // 試用天數文案。Public Release 初期 30 天，之後改回 5 天時只改這裡一個地方
  },

  // ---- 倒數計時 ----
  // 倒數到公測開始。時間一到，「距離公測開始還有」那塊會自動換成「公測已經開始」，
  // 不需要再改設定或重新部署。
  // +08:00 一定要留著 —— 拿掉的話瀏覽器會用訪客自己的時區解讀，
  // 不同時區的人看到的倒數會不一樣。
  countdown: {
    enabled: true,
    targetIso: "2026-08-27T12:00:00+08:00"   // 8/27（四）中午 12:00 台灣時間
  },

  // ---- 主 CTA ----
  cta: {
    mode: "form",                   // "form" = 開 Google 表單（Beta 階段）。"device" = 依裝置導對應商店（Public Release，尚未啟用）
    formBaseUrl: "https://forms.gle/oKRJeB39md3gFTvH9",
    iosUrl: null,                     // App Store 連結，Public Release 前補
    androidUrl: null                   // Google Play 連結，Public Release 前補
  },

  // ---- 頁尾 ----
  // 頁尾（treehouse 字標 + 隱私權政策／使用條款／刪除帳戶／常見問題 + 版權）。
  // Beta 期間先關掉，等 App 正式上線再打開 —— Morgan 2026-08-20 決定。
  // ⚠️ 打開之前請注意：本頁有載入 GA4，一般慣例會在頁面上留一個隱私權政策的入口。
  //    Beta 期間頁面本身不收資料（報名走 Google 表單），影響有限，但正式上線前
  //    這個 footer 應該要開回來。
  footer: {
    enabled: false
  },

  // ---- GA4 ----
  // 資源建立於公司帳號 admin@cherryontech.com。事件資料保留期限已設 14 個月。
  // 後台已註冊的自訂維度（事件範圍），參數名稱必須跟程式送出的 key 完全一致：
  //   cta_mode / utm_source / utm_medium / utm_campaign / utm_content / source
  // 留 null 就完全不載入 gtag.js，不會有任何對外連線。
  ga4: {
    measurementId: "G-J6XWVKCZXS"
  }
};
